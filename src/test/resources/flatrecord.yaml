openapi: 3.0.0
info: {title: flatrecord, version: "1.0"}
paths: {}
components:
  schemas:
    Widget:
      type: object
      required: [id]
      properties:
        id:
          type: string
        label:
          type: string
        size:
          type: integer
