---

---
/com
---
/com/example
---
/com/example/controller
---
/com/example/controller/Controller.java
package com.example.controller;

import com.example.dto.FileList;
import com.example.dto.TagList;
import com.example.dto.UploadMeta;
import java.lang.String;
import java.util.List;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestPart;
import org.springframework.web.multipart.MultipartFile;

public interface Controller {
  @PostMapping(
      value = "/upload",
      produces = "application/json",
      consumes = "multipart/form-data"
  )
  UploadMeta upload(@RequestPart(name = "one") MultipartFile one,
      @RequestPart(name = "files") List<MultipartFile> files,
      @RequestPart(name = "attachments") List<MultipartFile> attachments,
      @RequestPart(name = "alias_files") FileList alias_files,
      @RequestPart(name = "tags") List<String> tags,
      @RequestPart(name = "alias_tags") TagList alias_tags,
      @RequestPart(name = "meta") UploadMeta meta);
}
---
/com/example/dto
---
/com/example/dto/FileList.java
package com.example.dto;

import java.util.ArrayList;

public class FileList extends ArrayList<byte[]> {
}
---
/com/example/dto/TagList.java
package com.example.dto;

import java.lang.String;
import java.util.ArrayList;

public class TagList extends ArrayList<String> {
}
---
/com/example/dto/UploadMeta.java
package com.example.dto;

import com.fasterxml.jackson.databind.PropertyNamingStrategies;
import com.fasterxml.jackson.databind.annotation.JsonNaming;
import java.lang.String;
import java.util.List;
import lombok.Data;

@Data
@JsonNaming(PropertyNamingStrategies.SnakeCaseStrategy.class)
public class UploadMeta {
  private String comment;

  private byte[] thumbnail;

  private List<byte[]> blobs;
}
